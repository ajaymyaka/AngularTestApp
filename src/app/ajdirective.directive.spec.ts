import { AjdirectiveDirective } from './ajdirective.directive';

describe('AjdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AjdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
